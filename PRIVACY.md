# 隐私说明

Video Asset Organizer 是本地桌面软件，不包含广告、遥测、使用统计或自动上传功能。

## 本地数据

- 素材索引、人工整理信息和同步状态保存在 `%LOCALAPPDATA%\JianyingLiveOrganizer`。
- 日志保存在上述目录的 `logs` 子目录；日志会隐藏常见用户目录和凭据，不记录完整 FFmpeg 命令。
- 飞书 App Secret 仅保存在 Windows 凭据管理器，不会写入项目目录或普通设置文件。
- 扫描剪映草稿时只读取原文件；对于加密草稿，程序只处理临时副本，不回写原草稿。

## 网络访问

只有用户主动测试飞书连接或点击“同步飞书”时，软件才会连接飞书开放平台。发送内容由用户配置的字段映射决定。视频文件本身不会由本软件上传到飞书。

## 用户控制

用户可在“设置”中清除本机保存的飞书 App Secret。卸载软件不会自动删除素材索引；如需彻底移除，可手动删除 `%LOCALAPPDATA%\JianyingLiveOrganizer`。

