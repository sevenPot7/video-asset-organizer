# 干净电脑测试说明

## 启动

1. 将 ZIP 完整解压到本地文件夹，不要直接在压缩包内运行。
2. 双击 `VideoAssetOrganizer.exe`；不要单独复制 EXE，也不要删除 `_internal`。
3. 当前测试版尚未进行商业代码签名。如 Windows 显示未知发布者，请确认文件来源和 SHA-256 后再选择运行。

## 建议测试顺序

1. 不安装 Python，直接启动应用，确认没有 PowerShell/控制台黑框。
2. 点击“其他导入 → 导入普通视频文件夹”，选择少量普通视频，测试预览、场景说明和 Excel/CSV 导出。
3. 设置一个单独的输出目录，分别测试“精确剪切”和“快速剪切（不重新编码）”。
4. 如电脑装有剪映专业版，先复制一个草稿作为测试样本，再测试扫描；程序应只读草稿。
5. 使用测试用飞书多维表格配置 App ID、App Secret、App Token 和 Table ID，测试新增与更新。
6. 关闭再打开应用，检查素材索引与设置是否保留。

## 特别留意

- 中文 Windows 用户名、中文路径、空格路径是否正常。
- 没安装剪映时，普通视频功能应继续可用；加密草稿应给出明确提示。
- 飞书 App Secret 应保存在 Windows 凭据管理器，安装目录和 `settings.json` 中不应出现明文 Secret。
- 如遇问题，请保留错误截图和 `%LOCALAPPDATA%\JianyingLiveOrganizer\logs\app.log`。日志可能包含素材文件名，请在公开发送前检查。
